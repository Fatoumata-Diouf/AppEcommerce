server:
  port: 8082


spring:
  application:
    name: COMMANDE-SERVICE


  datasource:
    url: jdbc:h2:mem:commandedb
    driver-class-name: org.h2.Driver
    username: sa
    password:


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


  h2:
    console:
      enabled: true


eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka/

  instance:
    prefer-ip-address: true
